node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.*
!.env.example
.vercel

# Prisma generates the client into the source tree; `npm ci` regenerates it
# via the postinstall script, so it does not belong in git.
/lib/generated
next-env.d.ts
tsconfig.tsbuildinfo
